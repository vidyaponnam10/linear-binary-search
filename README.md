# linear-binary-search
Linear Search: Checks each element sequentially until the target is found or the list ends. Works on both sorted and unsorted arrays but can be slow for large data. Time complexity: O(n).Binary Search: Works only on sorted arrays. It finds the middle element and halves the search space repeatedly until the target is found. Time complexity: O(log n)
